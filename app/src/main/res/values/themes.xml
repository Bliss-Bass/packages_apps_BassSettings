<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BassSettings" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">?android:attr/textColorPrimary</item>
        <item name="android:textColorSecondary">?android:attr/textColorSecondary</item>

        <!-- We remove the circular colorSurface overrides to let the parent theme handle them,
             or you can map them to specific colors if preferred. -->
    </style>

    <style name="Theme.BassSettings.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
    <style name="Theme.BassSettings.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
</resources>
